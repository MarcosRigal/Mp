int *px; //Crea un puntero a un entero el valor, inicial es basura.
float a,b; //Crea dos variables de tipo float, el valor inicial es basura.
float *pa, *pb; //Crea dos punteros a flotantes, el valor inicial es basura.
float a =-0.167 //Crea una variable de tipo float, el valor inicial es 0.167.
float *pa = &a; //Crea un puntero de tipo float que apunta a la dirección de memoria de a.
char c1, c2, c3; //Crea tres caracteres de tipo char, el valor incial es basura.
char *pc1, *pc2, *pc3 = &c1; //Crea tres punteros de tipo char, el valor de los tres primeros es basura y el tercero apunta a c1.